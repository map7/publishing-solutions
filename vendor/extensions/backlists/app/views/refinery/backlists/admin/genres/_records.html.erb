<%= render 'refinery/admin/search_header', :url => refinery.backlists_admin_genres_path %>
<div class='pagination_container'>
  <% if @genres.any? %>
    <%= render 'genres' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
